import React from 'react';

import {{ properCase name }} from '.';
import { shallowWithStore } from 'services/enzyme';

describe('{{ properCase name }} component', () => {
  it('should exist', () => {
    const wrapper = shallowWithStore(<{{ properCase name }} />);

    expect(wrapper.exists()).toEqual(true);
  });
});
